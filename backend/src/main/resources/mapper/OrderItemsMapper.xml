<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.bjfu.nekocafe.mapper.OrderItemsMapper">
  <resultMap id="BaseResultMap" type="cn.edu.bjfu.nekocafe.entity.OrderItems">
    <id column="item_id" jdbcType="BIGINT" property="itemId" />
    <result column="reservation_id" jdbcType="BIGINT" property="reservationId" />
    <result column="dish_id" jdbcType="INTEGER" property="dishId" />
    <result column="quantity" jdbcType="INTEGER" property="quantity" />
    <result column="unit_price" jdbcType="NUMERIC" property="unitPrice" />
    <result column="subtotal" jdbcType="NUMERIC" property="subtotal" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    item_id, reservation_id, dish_id, quantity, unit_price, subtotal
  </sql>
  <select id="selectByExample" parameterType="cn.edu.bjfu.nekocafe.entity.OrderItemsExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from public.order_items
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from public.order_items
    where item_id = #{itemId,jdbcType=BIGINT}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from public.order_items
    where item_id = #{itemId,jdbcType=BIGINT}
  </delete>
  <delete id="deleteByExample" parameterType="cn.edu.bjfu.nekocafe.entity.OrderItemsExample">
    delete from public.order_items
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="cn.edu.bjfu.nekocafe.entity.OrderItems">
    insert into public.order_items (item_id, reservation_id, dish_id,
      quantity, unit_price, subtotal
      )
    values (#{itemId,jdbcType=BIGINT}, #{reservationId,jdbcType=BIGINT}, #{dishId,jdbcType=INTEGER},
      #{quantity,jdbcType=INTEGER}, #{unitPrice,jdbcType=NUMERIC}, #{subtotal,jdbcType=NUMERIC}
      )
  </insert>
  <insert id="insertSelective" parameterType="cn.edu.bjfu.nekocafe.entity.OrderItems">
    insert into public.order_items
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="itemId != null">
        item_id,
      </if>
      <if test="reservationId != null">
        reservation_id,
      </if>
      <if test="dishId != null">
        dish_id,
      </if>
      <if test="quantity != null">
        quantity,
      </if>
      <if test="unitPrice != null">
        unit_price,
      </if>
      <if test="subtotal != null">
        subtotal,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="itemId != null">
        #{itemId,jdbcType=BIGINT},
      </if>
      <if test="reservationId != null">
        #{reservationId,jdbcType=BIGINT},
      </if>
      <if test="dishId != null">
        #{dishId,jdbcType=INTEGER},
      </if>
      <if test="quantity != null">
        #{quantity,jdbcType=INTEGER},
      </if>
      <if test="unitPrice != null">
        #{unitPrice,jdbcType=NUMERIC},
      </if>
      <if test="subtotal != null">
        #{subtotal,jdbcType=NUMERIC},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="cn.edu.bjfu.nekocafe.entity.OrderItemsExample" resultType="java.lang.Long">
    select count(*) from public.order_items
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByExampleSelective" parameterType="map">
    update public.order_items
    <set>
      <if test="row.itemId != null">
        item_id = #{row.itemId,jdbcType=BIGINT},
      </if>
      <if test="row.reservationId != null">
        reservation_id = #{row.reservationId,jdbcType=BIGINT},
      </if>
      <if test="row.dishId != null">
        dish_id = #{row.dishId,jdbcType=INTEGER},
      </if>
      <if test="row.quantity != null">
        quantity = #{row.quantity,jdbcType=INTEGER},
      </if>
      <if test="row.unitPrice != null">
        unit_price = #{row.unitPrice,jdbcType=NUMERIC},
      </if>
      <if test="row.subtotal != null">
        subtotal = #{row.subtotal,jdbcType=NUMERIC},
      </if>
    </set>
    <if test="example != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByExample" parameterType="map">
    update public.order_items
    set item_id = #{row.itemId,jdbcType=BIGINT},
      reservation_id = #{row.reservationId,jdbcType=BIGINT},
      dish_id = #{row.dishId,jdbcType=INTEGER},
      quantity = #{row.quantity,jdbcType=INTEGER},
      unit_price = #{row.unitPrice,jdbcType=NUMERIC},
      subtotal = #{row.subtotal,jdbcType=NUMERIC}
    <if test="example != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByPrimaryKeySelective" parameterType="cn.edu.bjfu.nekocafe.entity.OrderItems">
    update public.order_items
    <set>
      <if test="reservationId != null">
        reservation_id = #{reservationId,jdbcType=BIGINT},
      </if>
      <if test="dishId != null">
        dish_id = #{dishId,jdbcType=INTEGER},
      </if>
      <if test="quantity != null">
        quantity = #{quantity,jdbcType=INTEGER},
      </if>
      <if test="unitPrice != null">
        unit_price = #{unitPrice,jdbcType=NUMERIC},
      </if>
      <if test="subtotal != null">
        subtotal = #{subtotal,jdbcType=NUMERIC},
      </if>
    </set>
    where item_id = #{itemId,jdbcType=BIGINT}
  </update>
  <update id="updateByPrimaryKey" parameterType="cn.edu.bjfu.nekocafe.entity.OrderItems">
    update public.order_items
    set reservation_id = #{reservationId,jdbcType=BIGINT},
      dish_id = #{dishId,jdbcType=INTEGER},
      quantity = #{quantity,jdbcType=INTEGER},
      unit_price = #{unitPrice,jdbcType=NUMERIC},
      subtotal = #{subtotal,jdbcType=NUMERIC}
    where item_id = #{itemId,jdbcType=BIGINT}
  </update>

  <!-- ========== 推荐模块自定义 SQL ========== -->

  <!-- R4: 用户历史点餐频次统计 -->
  <select id="selectDishFrequencyByUserId" resultType="java.util.Map">
    SELECT oi.dish_id AS dishid,
           SUM(oi.quantity) AS totalquantity
    FROM public.order_items oi
    WHERE oi.reservation_id IN (
        SELECT r.reservation_id
        FROM public.reservations r
        WHERE r.user_id = #{userId}
          AND r.status IN ('CONFIRMED'::reservation_status, 'COMPLETED'::reservation_status)
    )
    GROUP BY oi.dish_id
    ORDER BY totalquantity DESC
  </select>

  <!-- R17: 全店销量排行（兜底） -->
  <select id="selectGlobalDishRanking" resultType="java.util.Map">
    SELECT oi.dish_id AS dishid,
           COUNT(*) AS ordercount
    FROM public.order_items oi
    GROUP BY oi.dish_id
    ORDER BY ordercount DESC
    LIMIT #{limit}
  </select>

  <!-- R16: 同偏好协同过滤 — 找到口味偏好相同的其他用户最爱点的菜 -->
  <select id="selectCollaborativeFilterDishes" resultType="java.util.Map">
    SELECT oi.dish_id AS dishid,
           COUNT(*) AS ordercount
    FROM public.order_items oi
    INNER JOIN public.reservations r ON oi.reservation_id = r.reservation_id
    INNER JOIN public.member_ext me ON r.user_id = me.user_id
    WHERE r.user_id != #{currentUserId}
      AND me.preferences::text LIKE CONCAT('%', #{flavorPreference}, '%')
      AND r.status IN ('CONFIRMED'::reservation_status, 'COMPLETED'::reservation_status)
    GROUP BY oi.dish_id
    ORDER BY ordercount DESC
    LIMIT #{limit}
  </select>
</mapper>
