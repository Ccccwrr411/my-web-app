<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.bjfu.nekocafe.mapper.TableStatusMapper">
  <resultMap id="BaseResultMap" type="cn.edu.bjfu.nekocafe.entity.TableStatus">
    <id column="table_id" jdbcType="INTEGER" property="tableId" />
    <result column="status" jdbcType="VARCHAR" property="status" />
    <result column="current_reservation_id" jdbcType="BIGINT" property="currentReservationId" />
    <result column="version" jdbcType="INTEGER" property="version" />
  </resultMap>
  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Example_Where_Clause">
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    table_id, status, current_reservation_id, version
  </sql>
  <select id="selectByExample" parameterType="cn.edu.bjfu.nekocafe.entity.TableStatusExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from public.table_status
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from public.table_status
    where table_id = #{tableId,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from public.table_status
    where table_id = #{tableId,jdbcType=INTEGER}
  </delete>
  <delete id="deleteByExample" parameterType="cn.edu.bjfu.nekocafe.entity.TableStatusExample">
    delete from public.table_status
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="cn.edu.bjfu.nekocafe.entity.TableStatus">
    insert into public.table_status (table_id, status, current_reservation_id, 
      version)
    values (#{tableId,jdbcType=INTEGER}, #{status}::status_enum, #{currentReservationId,jdbcType=BIGINT}, 
      #{version,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="cn.edu.bjfu.nekocafe.entity.TableStatus">
    insert into public.table_status
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="tableId != null">
        table_id,
      </if>
      <if test="status != null">
        status,
      </if>
      <if test="currentReservationId != null">
        current_reservation_id,
      </if>
      <if test="version != null">
        version,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="tableId != null">
        #{tableId,jdbcType=INTEGER},
      </if>
      <if test="status != null">
        #{status}::status_enum,
      </if>
      <if test="currentReservationId != null">
        #{currentReservationId,jdbcType=BIGINT},
      </if>
      <if test="version != null">
        #{version,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <select id="countByExample" parameterType="cn.edu.bjfu.nekocafe.entity.TableStatusExample" resultType="java.lang.Long">
    select count(*) from public.table_status
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <update id="updateByExampleSelective" parameterType="map">
    update public.table_status
    <set>
      <if test="row.tableId != null">
        table_id = #{row.tableId,jdbcType=INTEGER},
      </if>
      <if test="row.status != null">
        status = #{row.status}::status_enum,
      </if>
      <if test="row.currentReservationId != null">
        current_reservation_id = #{row.currentReservationId,jdbcType=BIGINT},
      </if>
      <if test="row.version != null">
        version = #{row.version,jdbcType=INTEGER},
      </if>
    </set>
    <if test="example != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByExample" parameterType="map">
    update public.table_status
    set table_id = #{row.tableId,jdbcType=INTEGER},
      status = #{row.status}::status_enum,
      current_reservation_id = #{row.currentReservationId,jdbcType=BIGINT},
      version = #{row.version,jdbcType=INTEGER}
    <if test="example != null">
      <include refid="Update_By_Example_Where_Clause" />
    </if>
  </update>
  <update id="updateByPrimaryKeySelective" parameterType="cn.edu.bjfu.nekocafe.entity.TableStatus">
    update public.table_status
    <set>
      <if test="status != null">
        status = #{status}::status_enum,
      </if>
      <if test="currentReservationId != null">
        current_reservation_id = #{currentReservationId,jdbcType=BIGINT},
      </if>
      <if test="version != null">
        version = #{version,jdbcType=INTEGER},
      </if>
    </set>
    where table_id = #{tableId,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="cn.edu.bjfu.nekocafe.entity.TableStatus">
    update public.table_status
    set status = #{status}::status_enum,
      current_reservation_id = #{currentReservationId,jdbcType=BIGINT},
      version = #{version,jdbcType=INTEGER}
    where table_id = #{tableId,jdbcType=INTEGER}
  </update>

  <!-- 乐观锁更新：将桌位设为 RESERVED，version 自增（不要求 IDLE，支持不同时段多预约） -->
  <update id="reserveTableOptimistic">
    update public.table_status
    set status = 'RESERVED'::status_enum,
        current_reservation_id = #{reservationId,jdbcType=BIGINT},
        version = version + 1
    where table_id = #{tableId,jdbcType=INTEGER}
      and version = #{expectedVersion,jdbcType=INTEGER}
  </update>

  <!-- 乐观锁释放桌位：RESERVED → IDLE，current_reservation_id → NULL，version 自增 -->
  <update id="releaseTableOptimistic">
    update public.table_status
    set status = 'IDLE'::status_enum,
        current_reservation_id = NULL,
        version = version + 1
    where table_id = #{tableId,jdbcType=INTEGER}
      and status = 'RESERVED'::status_enum
      and version = #{expectedVersion,jdbcType=INTEGER}
  </update>

  <!-- 乐观锁占用桌位：RESERVED → OCCUPIED，version 自增（下单确认后） -->
  <update id="occupyTableOptimistic">
    update public.table_status
    set status = 'OCCUPIED'::status_enum,
        version = version + 1
    where table_id = #{tableId,jdbcType=INTEGER}
      and status = 'RESERVED'::status_enum
      and version = #{expectedVersion,jdbcType=INTEGER}
  </update>
</mapper>