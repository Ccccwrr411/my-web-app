# 2026-06-12 工作日志

## 店员工作台全面重构（前后端同步）

### 后端改动
1. **数据库迁移** V002：为 PostgreSQL `reservation_status` 枚举新增 MAKING、SERVING 值
2. **StaffService 接口** 新增 3 个方法：progressOrder(L-6)、getRefundList(L-7)、reviewRefund(L-8)
3. **StaffServiceImpl 实现**：
   - progressOrder：订单状态推进（CONFIRMED→MAKING→SERVING→COMPLETED），COMPLETED时桌位同步为CLEANING
   - getRefundList：查 refund_records 并关查 reservation 信息
   - reviewRefund：approve(退款通过+桌位释放) / reject(恢复预约状态)
   - toFrontendOrderStatus 新增 MAKING→making、SERVING→serving 映射
4. **StaffController** 新增 3 个端点：POST /staff/order/progress、GET /staff/refunds、POST /staff/refund/review
5. 后端编译验证通过

### 前端改动
1. **staff.wxml** 完全重写：4Tab底部导航布局（订单处理/桌位管理/告警中心/我的）
2. **staff.wxss** 完全重写：品牌色系、圆角卡片、毛玻璃底部导航、状态色彩编码
3. **staff.js** 完全重写：4Tab切换逻辑、订单状态筛选、进度推进、防误触二次确认、拒单原因填写
4. **staff.json** 更新：启用下拉刷新
5. **新增 pages/staff/refund** 4个文件：退款审核页（列表+筛选+通过/拒绝弹层）
6. **app.json** 注册 pages/staff/refund 路由
7. **mock.js** 补充 3 个新 API 的 mock 数据

### 设计要点
- 使用单页+自定义底部导航（微信全局tabBar已被客户页占用）
- 完整订单状态流转：待接单→已确认→制作中→已上菜→已完成
- 防误触：接单/进度推进/桌位调度均需二次确认，拒单需填原因
- 实时数据看板归店长/运营，店员工作台专注操作效率

## ReservationsMapper.xml 枚举类型转换 Bug 修复
- **问题**：MyBatis 用 `jdbcType=VARCHAR` 传 `status` 参数，但 PostgreSQL 列是 `reservation_status` 枚举类型，PreparedStatement 不自动转换报错
- **修复**：所有 INSERT/UPDATE 的 status 赋值改为 `#{status}::reservation_status` 显式类型转换
- **涉及 6 处**：insert(1)、insertSelective(1)、updateByExampleSelective(1)、updateByExample(1)、updateByPrimaryKeySelective(1)、updateByPrimaryKey(1)
- 编译验证通过

## 全量枚举 Mapper 修复（TableStatus + RefundRecords）
- **问题**：同 ReservationsMapper，TableStatusMapper 和 RefundRecordsMapper 的 status 字段也存在枚举类型不匹配
- **数据库枚举类型与表对应**（通过 psycopg2 直查确认）：
  - `reservations.status` → `reservation_status` ✅ 已修
  - `table_status.status` → `status_enum` ✅ 已修
  - `refund_records.status` → `refund_status_enum` ✅ 已修
- **非枚举字段（无需修改）**：payments.status、queue.status、reviews.status、shift_exceptions.status、user_coupons.status 均为普通 varchar
- 修复方式：所有 INSERT/UPDATE 的 status 赋值加 `::对应枚举类型名` 显式 CAST
- 编译验证通过

## V003 数据库迁移：status_enum 新增 CLEANING
- **问题**：progressOrder 在 COMPLETED 时设置桌位 status=CLEANING，但 status_enum 枚举只有 IDLE/RESERVED/OCCUPIED，CLEANING 不存在
- **修复**：ALTER TYPE status_enum ADD VALUE 'CLEANING'，已直接在数据库执行
- **迁移脚本**：V003__add_table_cleaning_status.sql 已创建
- status_enum 现有值：IDLE → RESERVED → OCCUPIED → CLEANING

## M1 用户与会员模块补全：注册/登录/实名认证

### 需求分析
- 任务书 M1：注册 / 登录 / 实名认证 / 会员等级 / 积分
- 原有只有微信一键登录，缺少手机号注册和手机号登录
- 实名认证后端已实现（/api/user/realname），前端 realname 页面也已实现，但 profile 页菜单入口已有
- 任务书鉴权要求：JWT + 短信验证码（沙箱）

### 后端改动
1. **pom.xml**：新增 jbcrypt 0.4 依赖（BCrypt 密码加密，不引入 spring-security 全家桶）
2. **RegisterDTO**：新建，含 phone/password/code 字段
3. **PhoneLoginDTO**：新建，含 phone/password 字段
4. **AuthService 接口**：新增 sendCode、register、phoneLogin 三个方法
5. **AuthServiceImpl**：
   - 注入 RedisTemplate，验证码存 Redis（key=`sms:{phone}`，5分钟过期）
   - sendCode：生成6位随机码 → 存 Redis → 沙箱模式直接返回给前端
   - register：校验验证码 → 查手机号唯一 → BCrypt加密密码 → 创建用户+积分记录 → 返回 JWT
   - phoneLogin：查手机号 → BCrypt校验密码 → 返回 JWT
   - 提取 buildLoginVO 工具方法，微信/注册/手机号登录共用
6. **AuthController**：新增 POST /send-code、POST /register、POST /login/phone 三个接口
7. **WebMvcConfig**：拦截器白名单新增 /api/auth/send-code、/api/auth/register、/api/auth/login/phone
8. 编译验证通过

### 前端改动
1. **login.wxml**：加 Tab 切换（微信登录/手机号登录），手机号登录面板含手机号+密码输入框+注册链接
2. **login.js**：新增 switchTab、onPhoneInput、onPasswordInput、onPhoneLogin、goRegister 方法
3. **login.wxss**：新增 .login-tabs、.phone-form、.register-link 等样式
4. **新建 pages/register/** 4个文件：注册页（手机号+验证码+密码+确认密码）
5. **request.js**：isLoginRequest 白名单新增 /api/auth/send-code、/api/auth/register、/api/auth/login/phone
6. **app.json**：注册 pages/register/register 路由
7. realname 页面无需改动（已有完整实现）

### M1 二轮修复（测试反馈）
1. **openid 未存 token**：buildLoginVO 方法增加 updateByPrimaryKeySelective 将 JWT 写入 users.openid 字段，注册/手机号登录/微信登录均生效
2. **RegisterDTO 新增字段**：nickname（必填）、email（选填），注册时不再硬编码"猫咖爱好者"
3. **验证码 UX 修复**：
   - 前端 canSubmit 只检查非空（不再强制 code.length===6），验证码正确性交给后端返回友好提示
   - 沙箱验证码显示在输入框下方（常驻可见），不只弹窗一次
4. 编译验证通过

### M1 三轮修复（实名认证缓存 Bug）
- **问题**：realname.js 的 `onLoad` 从本地缓存 `realnameInfo` 读实名数据，未按用户隔离，新用户登录后显示上一个用户的实名信息
- **修复**：
  - 后端 `UserProfileVO` 新增 `isVerified`、`realName`、`idCardMask` 字段
  - 后端 `UserServiceImpl.getProfile` 组装实名信息
  - 前端 `realname.js` 改为进入页面时调用 `/api/user/profile` 接口查询当前用户实名状态
  - 实名认证成功后更新本地 `userInfo` 缓存中的实名字段
- 编译验证通过

### 手机号正则放宽
- 原正则 `^1[3-9]\d{9}$` 不接受测试号（如11111111111），改为 `^1\d{10}$`（1开头+11位即可）
- 涉及5处：AuthController、AuthServiceImpl、login.js、register.js×2
- 编译验证通过

### 猫咪健康档案修复（D同学 I-1/I-2）
- **问题1**：数据库 `cat_health_records.record_type` 是大写（VACCINE/WEIGHT/INTERACTION），但后端代码用小写匹配 → 分组全失败，疫苗/体重/互动列表永远为空
- **问题2**：体重 `record_value` 带 `kg` 后缀（如 `4.6kg`），`Double.parseDouble()` 报错
- **问题3**：数据库无 INTERACTION 类型记录，互动时间线为空
- **问题4**：疫苗 note 缺少 nextDue 信息，无法判断疫苗状态
- **问题5**：前端显示"健康评分"但任务书未要求、数据库无此字段
- **修复**：
  - CatServiceImpl: `groupingBy` 改为 `r.getRecordType().toLowerCase()` 大小写不敏感
  - 体重解析: `recordValue.replaceAll("(?i)kg", "").trim()` 去掉 kg 后缀
  - 疫苗 nextDue: 正则提取 `nextDue=yyyy-MM-dd`，无 nextDue 时默认 status=valid
  - 互动 note: 支持 `mood=happy|描述` 格式
  - 移除健康评分卡片（前端 wxml + wxss）和后端 calcHealthScore/genHealthAdvice 方法
  - 前端疫苗/互动空列表加"暂无记录"提示
- **数据库补充**：4条疫苗nextDue更新 + 11条互动记录 + 10条体重记录（SQL脚本 seed_cat_health.sql）
  - ⚠️ 数据库连接数已满，SQL脚本待执行（需重启后端后再执行）
- 编译验证通过
2026-06-12: 执行 seed_cat_health.sql 成功 - 补充 INTERACTION 互动记录 22 条、WEIGHT 体重历史 23 条、VACCINE nextDue 更新 4 条

## 猫咪管家健康打卡功能（前后端）
- **问题**：猫咪管家(cat_keeper)登录后只能查看档案，和其他角色一样只读，无操作能力。isCatKeeper 前端变量设了但 WXML 未使用
- **后端改动**：
  1. 新建 `CatHealthRecordDTO`：catId, recordType, recordValue, note, recordDate, staffId
  2. `CatService` 接口新增 `addHealthRecord(CatHealthRecordDTO)` 方法
  3. `CatServiceImpl` 实现：参数校验 → 插入记录 → 体重类型同步更新 cat_profiles.weight_kg
  4. `StaffController` 新增 `POST /api/staff/cat/health` 端点
- **前端改动**：
  1. `cats.js`：引入 post 方法，新增打卡相关 data/methods（弹窗开关、类型切换、表单输入、心情选择、提交、详情刷新）
  2. `cats.wxml`：详情模式 +isCatKeeper 时显示浮动"+"按钮；新增底部弹窗表单（类型选择+对应字段+备注+提交）
  3. `cats.wxss`：新增 .fab-btn（浮动按钮）、.modal-mask/.modal-body（弹窗）、表单/心情选择/操作按钮样式
- 编译验证通过

## 猫咪管家健康打卡 Bug 修复系列
- **弹窗闪退**：FAB 按钮 bindtap 冒泡到 modal-mask 的 closeHealthForm → 改 catchtap 阻止冒泡，遮罩层去掉点击关闭，改用弹窗内 ✕ 按钮
- **Picker 类型选择器 Bug**：value 传了字符串而非数字索引 → 新增 typeIndex 数字变量，picker 绑 typeIndex
- **Canvas 遮挡弹窗**：微信小程序 canvas 原生层不受 CSS z-index 控制 → 加 wx:if="{{!showHealthForm}}" 打开弹窗时移除 canvas
- **打卡 500 错误**：insertSelective 缺少 useGeneratedKeys 配置，recordId 返回 null → Mapper XML 加 useGeneratedKeys；Map.of() 不允许 null 值 → 改 HashMap
- **疫苗表单逻辑修改**：拆分为"接种日期"（必填，默认今天）+"下次接种日期（选填）"；新增 healthFormVaccineDate 变量和 onVaccineDatePick 方法
- **healthFormVaccineDate 未在 data 初始化声明**：WXML 引用未声明变量导致页面白屏 → 在 data 中补充声明
- **体重趋势图不显示**：setData 后 setTimeout 画图时 canvas 节点可能未挂载 → 改为 setData 回调内 + 500ms 延迟确保 DOM 已更新
